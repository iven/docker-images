Debian-vagrant image
=============

What's modified:

* Supervisor is used to manage processes.
* SSH daemon is started by default.
* Vagrant user is created and [insecure keypair](https://github.com/mitchellh/vagrant/tree/master/keys) is enabled.
